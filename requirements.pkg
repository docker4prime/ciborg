# PURPOSE: os package dependencies for this project
# USAGE: apk update && apk add $(grep -vE "^(#.*|$)" requirements.pkg)

# minimal dependencies
bash
curl
git
python3
unzip

# project build dependencies
alpine-sdk
libffi-dev
openssl-dev
python3-dev
ncurses-dev
sqlite-dev
zlib-dev

# eof
